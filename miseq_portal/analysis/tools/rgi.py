"""
TODO: Implement RGI for AMR detection.

Example cmd:
rgi main -i {input_file} -i {outdir} --clean

RGI also has subcommands to generate visuals (parser, heatmap), create summaries for > 1 samples (tab)
"""
